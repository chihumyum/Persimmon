diff --git a/android/build.gradle b/android/build.gradle
index 6ff3a19b03c7a929664df3bc06b1ddd86ccb7670..3f5ad42eb42e42f56ddc583c4239afc3ced1ce4c 100644
--- a/android/build.gradle
+++ b/android/build.gradle
@@ -39,6 +39,7 @@ dependencies {
 
   implementation 'androidx.compose.foundation:foundation-android:1.10.6'
   implementation 'androidx.compose.ui:ui-android:1.10.6'
+  implementation 'androidx.activity:activity-compose:1.8.2'
   implementation "androidx.compose.material3:material3:1.5.0-alpha17"
   implementation 'androidx.compose.material3:material3-android:1.5.0-alpha17'
   implementation 'androidx.lifecycle:lifecycle-runtime:2.10.0'
diff --git a/android/src/main/java/expo/modules/ui/ExpoUIModule.kt b/android/src/main/java/expo/modules/ui/ExpoUIModule.kt
index ef400a6cd3b53f2e7185ad789c0f29fb09e7a1fe..f371bf050aecafa87a9aa269a06710c4401631ee 100644
--- a/android/src/main/java/expo/modules/ui/ExpoUIModule.kt
+++ b/android/src/main/java/expo/modules/ui/ExpoUIModule.kt
@@ -204,10 +204,18 @@ class ExpoUIModule : Module() {
       val hide by AsyncFunction()
       val expand by AsyncFunction()
       val partialExpand by AsyncFunction()
+      val onBackRequest by Event<Unit>()
       val onDismissRequest by Event<Unit>()
 
       Content { props ->
-        ModalBottomSheetContent(props, hide, expand, partialExpand) { onDismissRequest(Unit) }
+        ModalBottomSheetContent(
+          props,
+          hide,
+          expand,
+          partialExpand,
+          onDismissRequest = { onDismissRequest(Unit) },
+          onBackRequest = { onBackRequest(Unit) }
+        )
       }
     }
 
diff --git a/android/src/main/java/expo/modules/ui/ModalBottomSheetView.kt b/android/src/main/java/expo/modules/ui/ModalBottomSheetView.kt
index e90edd3fd682f1a53a0950526ae63fe20f3efd24..e9a3a86a60f3ffaef50ec6b5e34c79b89b9b2272 100644
--- a/android/src/main/java/expo/modules/ui/ModalBottomSheetView.kt
+++ b/android/src/main/java/expo/modules/ui/ModalBottomSheetView.kt
@@ -6,8 +6,15 @@ import android.app.Activity
 import android.app.Dialog
 import android.content.Context
 import android.graphics.Color
+import android.os.Build
 import android.view.KeyEvent
+import android.view.Window
 import android.view.inputmethod.InputMethodManager
+import androidx.activity.compose.BackHandler
+import androidx.compose.foundation.layout.WindowInsets
+import androidx.compose.foundation.layout.WindowInsetsSides
+import androidx.compose.foundation.layout.only
+import androidx.compose.foundation.layout.safeDrawing
 import androidx.compose.material3.BottomSheetDefaults
 import androidx.compose.material3.ExperimentalMaterial3Api
 import androidx.compose.material3.ModalBottomSheet
@@ -17,6 +24,7 @@ import androidx.compose.material3.rememberModalBottomSheetState
 import androidx.compose.runtime.Composable
 import androidx.compose.runtime.DisposableEffect
 import androidx.compose.runtime.LaunchedEffect
+import androidx.compose.runtime.SideEffect
 import androidx.compose.runtime.rememberCoroutineScope
 import androidx.compose.ui.platform.LocalView
 import androidx.compose.ui.window.DialogWindowProvider
@@ -30,6 +38,13 @@ import expo.modules.kotlin.views.OptimizedComposeProps
 import kotlinx.coroutines.withContext
 import kotlin.coroutines.cancellation.CancellationException
 
+private fun configureNavigationBar(window: Window) {
+  window.navigationBarColor = Color.TRANSPARENT
+  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
+    window.isNavigationBarContrastEnforced = false
+  }
+}
+
 @OptimizedRecord
 data class ModalBottomSheetPropertiesRecord(
   @Field val shouldDismissOnBackPress: Boolean = true,
@@ -43,6 +58,7 @@ data class ModalBottomSheetViewProps(
   val containerColor: Color? = null,
   val contentColor: Color? = null,
   val scrimColor: Color? = null,
+  val interceptBackPress: Boolean = false,
   val showDragHandle: Boolean = true,
   val sheetGesturesEnabled: Boolean = true,
   val properties: ModalBottomSheetPropertiesRecord = ModalBottomSheetPropertiesRecord(),
@@ -96,7 +112,8 @@ fun FunctionalComposableScope.ModalBottomSheetContent(
   hide: AsyncFunctionHandle<Unit>,
   expand: AsyncFunctionHandle<Unit>,
   partialExpand: AsyncFunctionHandle<Unit>,
-  onDismissRequest: () -> Unit
+  onDismissRequest: () -> Unit,
+  onBackRequest: () -> Unit
 ) {
   val sheetState = rememberModalBottomSheetState(props.skipPartiallyExpanded)
   val scope = rememberCoroutineScope()
@@ -156,11 +173,27 @@ fun FunctionalComposableScope.ModalBottomSheetContent(
       else -> null
     },
     properties = ModalBottomSheetProperties(
-      shouldDismissOnBackPress = props.properties.shouldDismissOnBackPress,
+      shouldDismissOnBackPress =
+        props.properties.shouldDismissOnBackPress && !props.interceptBackPress,
       shouldDismissOnClickOutside = props.properties.shouldDismissOnClickOutside
     ),
+    // The hosted React Native surface owns the bottom safe-area padding. Keep
+    // Material's top inset for a fully expanded sheet, but let the one animated
+    // surface draw continuously behind gesture navigation.
+    contentWindowInsets = { WindowInsets.safeDrawing.only(WindowInsetsSides.Top) },
     modifier = ModifierRegistry.applyModifiers(props.modifiers, appContext, composableScope, globalEventDispatcher)
   ) {
+    BackHandler(enabled = props.interceptBackPress) {
+      onBackRequest()
+    }
+    val view = LocalView.current
+    SideEffect {
+      var parent = view.parent
+      while (parent != null && parent !is DialogWindowProvider) {
+        parent = parent.parent
+      }
+      (parent as? DialogWindowProvider)?.window?.let(::configureNavigationBar)
+    }
     ForwardKeyEventsToActivity(appContext.currentActivity)
     Children(UIComposableScope(), filter = { !isSlotView(it) })
   }
diff --git a/build/jetpack-compose/ModalBottomSheet/index.d.ts b/build/jetpack-compose/ModalBottomSheet/index.d.ts
index ddf89aa5ac11610459f6db8abd96a3673f01054f..ceb651192b4bbde1023901921f943f37e936d4a7 100644
--- a/build/jetpack-compose/ModalBottomSheet/index.d.ts
+++ b/build/jetpack-compose/ModalBottomSheet/index.d.ts
@@ -44,6 +44,12 @@ export interface ModalBottomSheetProps {
      * (via swipe, back press, or tapping outside the scrim).
      */
     onDismissRequest: () => void;
+    /**
+     * Callback invoked for the Android system back action without dismissing the sheet.
+     * When supplied, it takes ownership of Back independently from swipe and scrim dismissal.
+     * @platform android
+     */
+    onBackRequest?: () => void;
     /**
      * Immediately opens the bottom sheet in full screen.
      * @default false
diff --git a/build/swift-ui/modifiers/presentationModifiers.d.ts b/build/swift-ui/modifiers/presentationModifiers.d.ts
index 36de99f5d92b1b0f2c340ba4508109f1a0b6045b..86867a437428ba3ae015d33265c6b370b700f6f5 100644
--- a/build/swift-ui/modifiers/presentationModifiers.d.ts
+++ b/build/swift-ui/modifiers/presentationModifiers.d.ts
@@ -17,6 +17,7 @@ export type PresentationDetent = 'medium' | 'large' | {
  * @param options - Optional settings for tracking the selected detent.
  * @param options.selection - The currently selected detent.
  * @param options.onSelectionChange - Callback fired when the user changes the active detent by dragging.
+ * @param options.allowsUserSelection - Whether dragging can select another detent.
  * @platform ios 16.0+
  * @platform tvos 16.0+
  * @see Official [SwiftUI documentation](https://developer.apple.com/documentation/swiftui/view/presentationdetents(_:selection:)).
@@ -24,6 +25,7 @@ export type PresentationDetent = 'medium' | 'large' | {
 export declare const presentationDetents: (detents: PresentationDetent[], options?: {
     selection?: PresentationDetent;
     onSelectionChange?: (detent: PresentationDetent) => void;
+    allowsUserSelection?: boolean;
 }) => ModifierConfig;
 /**
  * Controls the visibility of the drag indicator on a sheet.
diff --git a/expo-module.config.json b/expo-module.config.json
index 5627fcd9a1c515aaf3da2c3ce6dda92fbb851e21..0ebfddaa1dea66cfe4db659048f58939feae87a5 100644
--- a/expo-module.config.json
+++ b/expo-module.config.json
@@ -6,12 +6,6 @@
     "modules": ["ExpoUIModule"]
   },
   "android": {
-    "modules": ["expo.modules.ui.ExpoUIModule"],
-    "publication": {
-      "groupId": "expo.modules.ui",
-      "artifactId": "expo.modules.ui",
-      "version": "57.0.13",
-      "repository": "local-maven-repo"
-    }
+    "modules": ["expo.modules.ui.ExpoUIModule"]
   }
 }
diff --git a/ios/Modifiers/PresentationModifiers.swift b/ios/Modifiers/PresentationModifiers.swift
index f38528425a883a5d4dc0c1ad85e62dd278fc24b3..3022d2108d2cc1768e7e1ef0afedd39cb0488043 100644
--- a/ios/Modifiers/PresentationModifiers.swift
+++ b/ios/Modifiers/PresentationModifiers.swift
@@ -41,6 +41,7 @@ private func parsePresentationDetent(_ detent: Either<PresentationDetentPreset,
 internal struct PresentationDetentsModifier: ViewModifier, Record {
   @Field var detents: [Either<PresentationDetentPreset, PresentationDetentItem>]?
   @Field var selection: Either<PresentationDetentPreset, PresentationDetentItem>?
+  @Field var allowsUserSelection: Bool = true
   var eventDispatcher: EventDispatcher?
 
   init() {}
@@ -71,6 +72,7 @@ internal struct PresentationDetentsModifier: ViewModifier, Record {
         detents: parseDetents(),
         rawDetents: detents ?? [],
         initialSelection: selection.flatMap { parsePresentationDetent($0) },
+        allowsUserSelection: allowsUserSelection,
         eventDispatcher: eventDispatcher
       ) {
         content
@@ -107,6 +109,7 @@ private struct PresentationDetentsSelectionView<WrappedContent: View>: View {
   let detents: Set<PresentationDetent>
   let detentSerializationMap: [PresentationDetent: Any]
   let initialSelection: PresentationDetent?
+  let allowsUserSelection: Bool
   let eventDispatcher: EventDispatcher?
   let wrappedContent: WrappedContent
 
@@ -116,12 +119,14 @@ private struct PresentationDetentsSelectionView<WrappedContent: View>: View {
     detents: Set<PresentationDetent>,
     rawDetents: [Either<PresentationDetentPreset, PresentationDetentItem>],
     initialSelection: PresentationDetent?,
+    allowsUserSelection: Bool,
     eventDispatcher: EventDispatcher?,
     @ViewBuilder content: () -> WrappedContent
   ) {
     self.detents = detents
     self.detentSerializationMap = buildDetentSerializationMap(from: rawDetents)
     self.initialSelection = initialSelection
+    self.allowsUserSelection = allowsUserSelection
     self.eventDispatcher = eventDispatcher
     self.wrappedContent = content()
     // Default to the first detent in the ordered array, not `detents.first` (the Set's order is
@@ -130,9 +135,17 @@ private struct PresentationDetentsSelectionView<WrappedContent: View>: View {
     self._selectedDetent = State(initialValue: initialSelection ?? firstOrderedDetent ?? .large)
   }
 
+  private var activeDetents: Set<PresentationDetent> {
+    allowsUserSelection ? detents : [selectedDetent]
+  }
+
   var body: some View {
     wrappedContent
-      .presentationDetents(detents, selection: $selectedDetent)
+      // A guarded Binding still lets UISheetPresentationController drag among
+      // every advertised detent before SwiftUI rejects the final write. When
+      // selection is locked, advertise only the programmatically selected
+      // detent so no alternate height exists for the gesture recognizer.
+      .presentationDetents(activeDetents, selection: $selectedDetent)
       .onChange(of: selectedDetent) { newDetent in
         if let serialized = detentSerializationMap[newDetent] {
           eventDispatcher?(["presentationDetents": ["detent": serialized]])
@@ -140,7 +153,9 @@ private struct PresentationDetentsSelectionView<WrappedContent: View>: View {
       }
       .onChange(of: initialSelection) { newSelection in
         if let newSelection {
-          selectedDetent = newSelection
+          withAnimation(.easeInOut(duration: 0.24)) {
+            selectedDetent = newSelection
+          }
         }
       }
   }
diff --git a/src/community/bottom-sheet/BottomSheet.ios.tsx b/src/community/bottom-sheet/BottomSheet.ios.tsx
index edd61228b9881de7ab542e535124fef67fc0b6ec..4083e3f2978d5ee0d28ba96a06030dd43ed5cce4 100644
--- a/src/community/bottom-sheet/BottomSheet.ios.tsx
+++ b/src/community/bottom-sheet/BottomSheet.ios.tsx
@@ -89,6 +89,8 @@ export function BottomSheet(props: BottomSheetProps) {
     onClose,
     onDismiss,
     enablePanDownToClose = false,
+    enableContentPanningGesture = true,
+    enableHandlePanningGesture = true,
     enableDynamicSizing = true,
     handleComponent,
     backgroundStyle,
@@ -205,6 +207,8 @@ export function BottomSheet(props: BottomSheetProps) {
             presentationDetents(detents, {
               selection: selectedDetent,
               onSelectionChange: handleDetentChange,
+              allowsUserSelection:
+                enableContentPanningGesture && enableHandlePanningGesture,
             }),
           ]),
       presentationDragIndicator(handleComponent === null ? 'hidden' : 'visible'),
@@ -218,6 +222,8 @@ export function BottomSheet(props: BottomSheetProps) {
     handleDetentChange,
     handleComponent,
     enablePanDownToClose,
+    enableContentPanningGesture,
+    enableHandlePanningGesture,
     backgroundStyle,
   ]);
 
diff --git a/src/jetpack-compose/ModalBottomSheet/index.tsx b/src/jetpack-compose/ModalBottomSheet/index.tsx
index ea749156c15b4929ac692dc0a513d7b00430552c..504871d9310383ebd2d3801cf29e6b596863e14d 100644
--- a/src/jetpack-compose/ModalBottomSheet/index.tsx
+++ b/src/jetpack-compose/ModalBottomSheet/index.tsx
@@ -57,6 +57,12 @@ export interface ModalBottomSheetProps {
    * (via swipe, back press, or tapping outside the scrim).
    */
   onDismissRequest: () => void;
+  /**
+   * Callback invoked for the Android system back action without dismissing the sheet.
+   * When supplied, it takes ownership of Back independently from swipe and scrim dismissal.
+   * @platform android
+   */
+  onBackRequest?: () => void;
   /**
    * Immediately opens the bottom sheet in full screen.
    * @default false
@@ -101,7 +107,12 @@ export interface ModalBottomSheetProps {
   modifiers?: ModifierConfig[];
 }
 
-type NativeModalBottomSheetProps = Omit<ModalBottomSheetProps, 'onDismissRequest'> & {
+type NativeModalBottomSheetProps = Omit<
+  ModalBottomSheetProps,
+  'onDismissRequest' | 'onBackRequest'
+> & {
+  interceptBackPress: boolean;
+  onBackRequest?: () => void;
   onDismissRequest: () => void;
 };
 
@@ -111,13 +122,20 @@ const ModalBottomSheetNativeView: ComponentType<NativeModalBottomSheetProps> = r
 );
 
 function transformProps(props: ModalBottomSheetProps): NativeModalBottomSheetProps {
-  const { modifiers, onDismissRequest, ...restProps } = props;
+  const { modifiers, onBackRequest, onDismissRequest, ...restProps } = props;
   return {
     modifiers,
     ...(modifiers ? createViewModifierEventListener(modifiers) : undefined),
     ...restProps,
     skipPartiallyExpanded: props.skipPartiallyExpanded ?? false,
     initialFullyExpanded: props.initialFullyExpanded ?? false,
+    interceptBackPress: onBackRequest !== undefined,
+    onBackRequest:
+      onBackRequest === undefined
+        ? undefined
+        : () => {
+            onBackRequest();
+          },
     onDismissRequest: () => {
       onDismissRequest?.();
     },
diff --git a/src/swift-ui/modifiers/presentationModifiers.ts b/src/swift-ui/modifiers/presentationModifiers.ts
index a73238902a2163cadb800ddd24bd031b5725f7e3..3d5a9a5dcab46813b37e1cd1442b8aac395ddb82 100644
--- a/src/swift-ui/modifiers/presentationModifiers.ts
+++ b/src/swift-ui/modifiers/presentationModifiers.ts
@@ -19,6 +19,7 @@ export type PresentationDetent = 'medium' | 'large' | { fraction: number } | { h
  * @param options - Optional settings for tracking the selected detent.
  * @param options.selection - The currently selected detent.
  * @param options.onSelectionChange - Callback fired when the user changes the active detent by dragging.
+ * @param options.allowsUserSelection - Whether dragging can select another detent.
  * @platform ios 16.0+
  * @platform tvos 16.0+
  * @see Official [SwiftUI documentation](https://developer.apple.com/documentation/swiftui/view/presentationdetents(_:selection:)).
@@ -28,9 +29,14 @@ export const presentationDetents = (
   options?: {
     selection?: PresentationDetent;
     onSelectionChange?: (detent: PresentationDetent) => void;
+    allowsUserSelection?: boolean;
   }
 ): ModifierConfig => {
-  const params = { detents, selection: options?.selection };
+  const params = {
+    detents,
+    selection: options?.selection,
+    allowsUserSelection: options?.allowsUserSelection ?? true,
+  };
 
   const { onSelectionChange } = options ?? {};
   if (onSelectionChange) {
